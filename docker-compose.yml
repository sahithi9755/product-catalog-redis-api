
services:
  api-service:
    build: .
    ports:
      - "8080:8080"
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - CACHE_TTL_SECONDS=3600
    depends_on:
      - redis

  redis:
    image: redis:6-alpine
    ports:
      - "6379:6379"